server {
    listen 80;

    # Proxy to monitor service, strip /monitor from the path
    location /monitor/ {
        proxy_pass http://monitor:5001/;  # Note the trailing slash!
    }

    # Proxy to board service
    location / {
        proxy_pass http://board:5000/;
    }
}

